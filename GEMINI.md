# Fail2Ban Prometheus Exporter

Read `AGENTS.md` for the project goal, architecture, commands, and mandatory
contracts. Read the applicable `.gemini/rules/*.md` files explicitly; they mirror
the canonical `.claude/rules/*.md` rules. Keep shared project facts in `AGENTS.md`.
